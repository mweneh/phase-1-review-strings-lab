// Write your code in this file!
let currentUser="You need to modify the value of the 'currentUser' variable"
let welcomeMessage= "Welcome to Flatbook, You need to modify the value of the 'currentUser' variable!"

const upperCaseCurrentUser = currentUser.toUpperCase();
let excitedWelcomeMessage= `WELCOME TO FLATBOOK, ${upperCaseCurrentUser}!`
const firstInitial = currentUser[0];
const restOfName = currentUser.slice(1);
let shortGreeting= 'Welcome, Y!'